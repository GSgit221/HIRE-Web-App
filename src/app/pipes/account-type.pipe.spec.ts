/* tslint:disable:no-unused-variable */
import { AccountTypePipe } from './account-type.pipe';

describe('Pipe: AccountTypee', () => {
    it('create an instance', () => {
        const pipe = new AccountTypePipe();
        expect(pipe).toBeTruthy();
    });
});
