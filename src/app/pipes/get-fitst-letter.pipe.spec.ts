import { GetFitstLetterPipe } from './get-fitst-letter.pipe';

describe('GetFitstLetterPipe', () => {
  it('create an instance', () => {
    const pipe = new GetFitstLetterPipe();
    expect(pipe).toBeTruthy();
  });
});
